# Dependencies for the SEIR well-mixed milestone.
numpy>=1.24
matplotlib>=3.7

# Saving the animation as a GIF works out of the box via matplotlib's Pillow
# writer (Pillow ships with matplotlib).

# Not needed yet: networkx / scipy return in the next milestone, when the
# well-mixed interaction layer is replaced by a Watts-Strogatz contact network.
