# Milestone 1 dependencies
networkx>=3.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Optional? saving animations as MP4 requires an ffmpeg binary on PATH.
# GIF export works out of the box via matplotlib's Pillow writer.
